{
  "name": "parcel-example",
  "version": "0.0.1",
  "description": "parcel bundler example",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r ./dist && parcel build index.html",
    "dev": "rm -r ./dist && parcel index.html",
    "build:watch": "rm -r ./dist parcel watch index.html"
  },
  "keywords": [
    "parcel",
    "javascript",
    "bundling",
    "bundler",
    "bundle"
  ],
  "author": "kern",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "jquery": "^3.2.1",
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.0.3"
  }
}
